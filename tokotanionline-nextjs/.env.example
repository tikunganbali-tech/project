# =============================================================================
# TOKO TANI ONLINE - ENVIRONMENT VARIABLES TEMPLATE
# =============================================================================
# 
# INSTRUKSI:
# 1. Copy file ini menjadi .env (untuk development) atau .env.local
# 2. Isi semua nilai yang diperlukan (jangan commit file .env ke git!)
# 3. Untuk production, set environment variables di hosting platform
#
# KEAMANAN:
# - JANGAN commit file .env ke repository
# - JANGAN share nilai rahasia (secrets, API keys, passwords)
# - Default values di sini adalah AMAN (engine/marketing OFF)
# =============================================================================

# =============================================================================
# CORE SYSTEM
# =============================================================================

# Node environment: development | production | test
# Default: development (untuk safety)
NODE_ENV=development

# Base URL aplikasi (untuk API calls internal)
# Contoh: http://localhost:3000 (dev) atau https://tokotanionline.com (prod)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Site URL untuk SEO (sitemap, robots.txt, meta tags)
# Contoh: https://tokotanionline.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# =============================================================================
# DATABASE
# =============================================================================

# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
# REQUIRED: Harus diisi untuk aplikasi bisa jalan
DATABASE_URL=postgresql://user:password@localhost:5432/tokotanionline

# =============================================================================
# AUTHENTICATION (NextAuth)
# =============================================================================

# NextAuth secret untuk encrypt session tokens
# REQUIRED di production
# Generate dengan: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-key-here-generate-with-openssl-rand-base64-32

# NextAuth URL (base URL untuk auth callbacks)
# Default: akan menggunakan NEXT_PUBLIC_BASE_URL jika tidak di-set
NEXTAUTH_URL=http://localhost:3000

# =============================================================================
# ENGINE INFRASTRUCTURE
# =============================================================================
# âš ï¸  PENTING: Default semua engine/marketing = OFF untuk safety

# Engine Hub URL (Go engine service)
# Default: localhost:8080 (jika engine-hub berjalan lokal)
# Set ke empty string untuk disable engine hub
ENGINE_HUB_URL=http://localhost:8080

# Go Engine API URL (alternate/legacy)
# Default: localhost:8080
GO_ENGINE_API_URL=http://localhost:8080

# =============================================================================
# MARKETING & AI ENGINE (KILL SWITCH)
# =============================================================================
# âš ï¸  CRITICAL: Default semua = OFF untuk mencegah accidental live dispatch

# Global marketing live mode
# false = DRY-RUN (safe, hanya log, tidak kirim ke platform)
# true = LIVE (bahaya jika aktif tanpa testing!)
# DEFAULT: false (WAJIB false di production default)
MARKETING_LIVE_ENABLED=false

# Global marketing dry-run mode
# true = dry-run (safe)
# false = live (bahaya!)
# DEFAULT: true (WAJIB true di production default)
MARKETING_DRY_RUN=true

# Event allowlist untuk live mode (comma-separated)
# Contoh: "purchase,add_to_cart"
# Kosong = allow all events (jika live enabled)
# DEFAULT: kosong (safe)
MARKETING_LIVE_EVENTS=

# Per-integration dry-run overrides
# true = dry-run untuk integration ini
# false = live untuk integration ini
# DEFAULT: true (safe)
FB_DRY_RUN=true
GA_DRY_RUN=true
TIKTOK_DRY_RUN=true

# =============================================================================
# EMAIL / SMTP
# =============================================================================

# SMTP server configuration
# Optional: Hanya diperlukan jika fitur email aktif
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_SECURE=false
EMAIL_FROM=your-email@gmail.com

# =============================================================================
# THIRD-PARTY SERVICES (MARKETING / TRACKING)
# =============================================================================
# Optional: Hanya diperlukan jika menggunakan tracking pixels

# Facebook Pixel ID
NEXT_PUBLIC_FACEBOOK_PIXEL_ID=

# Google Ads Conversion ID
NEXT_PUBLIC_GOOGLE_ADS_ID=

# Google Analytics 4 ID
NEXT_PUBLIC_GA4_ID=

# TikTok Pixel ID
NEXT_PUBLIC_TIKTOK_PIXEL_ID=

# =============================================================================
# IMAGE GENERATION API KEYS
# =============================================================================
# Optional: Hanya diperlukan jika menggunakan image generation services

# Pexels API Key
PEXELS_API_KEY=

# Pixabay API Key
PIXABAY_API_KEY=

# Unsplash Access Key
UNSPLASH_ACCESS_KEY=

# Stable Diffusion API Key
STABLE_DIFFUSION_API_KEY=

# Leonardo AI API Key
LEONARDO_API_KEY=

# =============================================================================
# INTERNAL SERVICES
# =============================================================================

# Internal event API key (untuk engine-hub communication)
# Generate dengan: openssl rand -base64 32
# REQUIRED jika menggunakan engine-hub
INTERNAL_EVENT_KEY=your-internal-event-key-generate-with-openssl-rand-base64-32

# =============================================================================
# WHATSAPP INTEGRATION
# =============================================================================

# WhatsApp number untuk CTA button (format: 6281234567890)
# Optional: Hanya diperlukan jika menggunakan WhatsApp CTA
NEXT_PUBLIC_WHATSAPP_NUMBER=

# =============================================================================
# TESTING (Development Only)
# =============================================================================
# âš ï¸  JANGAN gunakan di production!

# Test admin credentials (untuk smoke tests)
# Hanya untuk development/testing
TEST_ADMIN_EMAIL=admin@tokotanionline.com
TEST_ADMIN_PASSWORD=admin123

# =============================================================================
# AUTO-SET VARIABLES (Tidak perlu di-set manual)
# =============================================================================
# Variabel berikut di-set otomatis oleh platform/runtime:

# VERCEL_URL - Auto-set oleh Vercel (jika deploy di Vercel)
# NEXT_RUNTIME - Auto-set oleh Next.js (nodejs | edge)

# =============================================================================
# PRODUCTION CHECKLIST
# =============================================================================
# Sebelum deploy ke production, pastikan:
#
# âœ… NODE_ENV=production
# âœ… NEXTAUTH_SECRET di-set dengan nilai random yang kuat
# âœ… DATABASE_URL di-set dengan connection string production
# âœ… MARKETING_LIVE_ENABLED=false (atau true hanya setelah testing menyeluruh)
# âœ… MARKETING_DRY_RUN=true (atau false hanya setelah testing menyeluruh)
# âœ… Semua API keys di-set dengan nilai production
# âœ… SMTP credentials di-set dengan nilai production
# âœ… INTERNAL_EVENT_KEY di-set dengan nilai random yang kuat
# âœ… NEXT_PUBLIC_BASE_URL dan NEXT_PUBLIC_SITE_URL di-set ke domain production
# âœ… Semua NEXT_PUBLIC_* variables di-set sesuai kebutuhan
#
# =============================================================================
